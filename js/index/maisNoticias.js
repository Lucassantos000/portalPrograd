
async function maisNoticias(element){
    console.log(element);

    // O ATRIBUTO PAGINATION SERVE PARA MOSTAR A PAGINAÇÃO DAS NOTÍCIA NA PÁGINA, (INICIALMENTE )
    element.getAttribute('pagination');
}